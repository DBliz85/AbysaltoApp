spring.application.name=java.mid

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/abysalto_database?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/changelog-master.xml

logging.level.org.springframework.security=DEBUG

jwt.secret=${JWT_SECRET:VeRyL0ngAndSecureJWTSecretKey123454321}
jwt.expiration=${JWT_EXPIRATION:3600000}